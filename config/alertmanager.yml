receivers:
  - name: 'email-notifications'
    email_configs:
      - to: 'lykhun@gmail.com'
        from: 'AlertManager <khun@khunly.be>'
        smarthost: 'ssl0.ovh.net:587'
        auth_username: 'khun@khunly.be'
        auth_password: 'test1234*'
        require_tls: true

route:
  receiver: email-notifications
  group_interval: 5m
  repeat_interval: 3h
